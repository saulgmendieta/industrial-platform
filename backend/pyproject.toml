[project]
name = "industrial-monitoring-api"
version = "0.1.0"
description = "Backend API for an industrial monitoring platform"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]"
]
